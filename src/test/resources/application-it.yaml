spring:
  datasource:
    hikari:
      schema: catalogsvc
    url: jdbc:tc:postgresql:15:///polar_db?currentSchema=catalogsvc

catalog:
  test-data:
    enabled: true

logging:
  level:
    com.bazinga.eg.catalogservice: debug
    org.springframework.boot: debug
    org.springframework.web: debug